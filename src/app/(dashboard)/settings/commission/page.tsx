import { PageStub } from "@/components/widgets/page-stub";

export default function CommissionSettingsPage(): React.ReactElement {
  return (
    <PageStub
      title="Commission"
      description="Sales commission rate and eligibility rules used in profit math."
    />
  );
}
