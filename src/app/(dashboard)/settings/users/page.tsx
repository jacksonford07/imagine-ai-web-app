import { PageStub } from "@/components/widgets/page-stub";

export default function UsersSettingsPage(): React.ReactElement {
  return (
    <PageStub
      title="Users & Roles"
      description="Add users by email, assign Admin / Editor / Viewer, toggle Fulfillment access."
    />
  );
}
