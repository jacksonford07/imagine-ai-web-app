import { PageStub } from "@/components/widgets/page-stub";

export default function IntegrationsSettingsPage(): React.ReactElement {
  return (
    <PageStub
      title="Integrations"
      description="Per-connector sync status, record counts, backfill progress, and manual refresh."
    />
  );
}
