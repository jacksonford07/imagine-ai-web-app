import { PageStub } from "@/components/widgets/page-stub";

export default function AlertsSettingsPage(): React.ReactElement {
  return (
    <PageStub
      title="Alerts"
      description="Thresholds and Slack routing for KPI and sync-failure alerts."
    />
  );
}
