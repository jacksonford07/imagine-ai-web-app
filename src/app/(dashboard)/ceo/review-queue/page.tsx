import { PageStub } from "@/components/widgets/page-stub";

export default function ReviewQueuePage(): React.ReactElement {
  return (
    <PageStub
      title="Review Queue"
      description="Unmatched cross-platform customers awaiting a merge / keep-separate decision."
    />
  );
}
