import { PageStub } from "@/components/widgets/page-stub";

export default function TransactionsPage(): React.ReactElement {
  return (
    <PageStub
      title="Transactions"
      description="Every ingested transaction with fees, adjustments, and overrides."
    />
  );
}
