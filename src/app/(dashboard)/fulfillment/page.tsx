import { PageStub } from "@/components/widgets/page-stub";

export default function FulfillmentOverviewPage(): React.ReactElement {
  return (
    <PageStub
      title="Fulfillment"
      description="Bot operations at a glance — students, escalations, message volume."
    />
  );
}
